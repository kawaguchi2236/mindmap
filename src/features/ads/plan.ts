/**
 * 広告を出す相手かどうかの判定（task.md §14 / CLAUDE.md §15）。
 *
 * Phase 1 に課金の仕組みは無い。CLAUDE.md §3.2 が「有料課金」と「Pro のチェックアウト」を
 * 明確に対象外にしているため、プランの状態管理・プラン種別の型・課金を見越した
 * インターフェースはここに作らない（CLAUDE.md §37）。判定はこの1関数だけで足りる。
 */

/**
 * この閲覧者に広告を出してよいか。
 *
 * Phase 1 はゲストもログイン中のユーザーも全員 Free なので、常に true。
 * ログインの有無で出し分ける予定は無い（ログインしたら広告が消える、という
 * 約束を Phase 1 はしていない）。
 *
 * それでも `signedIn` を受け取っているのは、呼び出し側に「広告を出すかは
 * ここが決める」と示すため。将来 Pro を足すときは、この関数の中だけを
 * 変えれば済む（呼び出し側の画面は触らない）。
 *
 * 引数が `SessionUser | null` ではなく boolean なのは、マップ一覧画面
 * （`MapListScreen`）がセッションから受け取っているのが `signedIn: boolean` だけで、
 * ユーザー本体を持っていないため。Phase 1 で判定に使える事実はログインの有無しかない。
 */
export function shouldShowAds(_signedIn: boolean): boolean {
  return true;
}
